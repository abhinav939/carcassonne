import Player from "./player"
import { Tile } from "./tile"
const readline = require('readline');

// A Move happens once a player has drawn a tile from the stack
class Move {  //takes in as arguments: vector, rotation
    constructor() {

    }


    //      Player places tile (validate)
    //      Player places Meeple (validate)
}
export default Move
